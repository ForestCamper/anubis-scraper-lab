# Scrapy project config for the Extraction Range flag crawler.
# `scrapy crawl flags` works from this directory; the launcher
# (scraper_scrapy.py) also reads these settings via get_project_settings().
[settings]
default = flagcrawler.settings

[deploy]
project = flagcrawler
